<% content_for :title, "Add Someone" %>

<%= link_to users_path, class: "btn btn-sm btn-outline-secondary mb-3" do %>
  <i class="bi bi-arrow-left me-1"></i> Back
<% end %>

<div class="form-card">
  <h1 class="h4 fw-bold mb-1">Add to the Crew 👋</h1>
  <p class="text-muted small mb-4">Who's joining the table?</p>
  <%= render "form", user: @user %>
</div>
