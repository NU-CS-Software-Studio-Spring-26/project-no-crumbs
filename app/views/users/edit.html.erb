<% content_for :title, "Edit Profile" %>

<%= link_to @user, class: "btn btn-sm btn-outline-secondary mb-3" do %>
  <i class="bi bi-arrow-left me-1"></i> Back
<% end %>

<div class="form-card">
  <h1 class="h4 fw-bold mb-4">Edit Profile</h1>
  <%= render "form", user: @user %>
</div>
