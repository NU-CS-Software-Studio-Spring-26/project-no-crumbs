<% content_for :title, "Page not found — No Crumbs" %>

<div class="empty-state" style="min-height: 60vh; justify-content: center;">
  <div class="empty-icon">🍽️</div>
  <p class="empty-title">Nothing on the menu here</p>
  <p class="empty-sub">This page doesn't exist or may have moved.</p>
  <%= link_to "Back to NoCrumbs", root_path, class: "btn-brand-nc mt-3" %>
</div>
