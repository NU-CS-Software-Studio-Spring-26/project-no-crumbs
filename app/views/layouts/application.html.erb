<!DOCTYPE html>
<html lang="en">
  <head>
    <title>NoCrumbs</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:ital,wght@0,400;0,500;0,600;0,700;0,800;1,400&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
<%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="nc-nav">
      <div class="nc-nav-left">
        <%= link_to root_path, class: "nc-brand" do %>
          No<span>Crumbs</span>
        <% end %>
      </div>
      <div class="nc-nav-mid">
        <%= link_to "Meals", posts_path, class: "nc-nav-link" %>
        <%= link_to "People", users_path, class: "nc-nav-link" %>
      </div>
      <div class="nc-nav-right">
        <% if user_signed_in? %>
          <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete }, class: "nc-nav-link" %>
          <%= link_to new_post_path, class: "nc-nav-cta" do %>
            <i class="bi bi-plus"></i> Plan a Meal
          <% end %>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "nc-nav-link" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "nc-nav-cta" %>
        <% end %>
      </div>
    </nav>

    <div class="nc-container">
      <% if notice.present? %>
        <div class="nc-flash">
          <i class="bi bi-check-circle-fill"></i>
          <%= notice %>
        </div>
      <% end %>
      <% if alert.present? && !devise_controller? %>
        <div class="nc-flash nc-flash-alert">
          <i class="bi bi-exclamation-circle-fill"></i>
          <%= alert %>
        </div>
      <% end %>
      <%= yield %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
